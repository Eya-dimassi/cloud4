spring:
  rabbitmq:
    host: "localhost"
    port: 5672
    username: "guest"
    password: "guest"
  application:
    name: "configserver"
  profiles:
    # active: native
    active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Eya-dimassi/config-repo
          default-label: main
          clone-on-start: true

        # native:
          # search-locations: "classpath:/config"
          # search-locations: file:///C:/Angular/eya-spring-cloud/config-repo

server:
  port: 9999
management:
  endpoints:
    web:
      exposure:
        include: '*'